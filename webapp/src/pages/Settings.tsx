// Hardcoded version, no backend integration
export default function Settings() {
  return (
    <div>
      <h2>Settings</h2>
      <p>Version: v0.0.1 — "Stable"</p>
      <select>
        <option>Option A</option>
        <option>Option B</option>
      </select>
    </div>
  );
}
